### 핵심 아이디어
- 정렬된 배열에서 연속된 숫자 구간을 찾아 문자열로 압축한다.
- 하나의 구간이 시작되면, 연속이 끊길 때까지 오른쪽으로 이동한다.

### 패턴
- 구간 탐색
- Two pointer 느낌
	- start_idx: 현재 구간의 시작
	- idx: 현재 구간의 끝을 찾는 포인터

### 이 패턴을 떠올려야 하는 신호
- 연속된 값들을 하나의 구간으로 묶어야 한다.
- 배열을 왼쪽부터 한 번만 순회해도 된다.
- 현재 값과 다음 값의 관계를 비교한다.
