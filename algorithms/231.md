## 핵심 아이디어
- 2의 거듭제곱의 특성

## 패턴
- 비트로 접근
- 2의 거듭제곱: 2진수가 MSB가 1, 나머지가 0으로 이루어짐
	- n이 2의 제곱수라면, 100..000 꼴로 이루어짐
	  n-1은 011..111 꼴로 이루어짐
	- 따라서 n & (n-1) == 0 이 됨
- 예외: n = 0 은 2의 제곱수이지만, 위 비트 계산을 만족하지는 않음

## 활용
- n & (n-1): 가장 낮은 1비트 제거
- n & -n: 가장 낮은 1비트 추출
- 2의 거듭제곱 판별
- 비트 개수 세기
- bitmasking
